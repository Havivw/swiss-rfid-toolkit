App(
    appid="reader_audit",
    name="Reader Audit",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="reader_audit_app",
    stack_size=4 * 1024,
    fap_category="NFC",
    fap_description="Reader audit v2: detect an HF/LF reader field, then emulate a card and classify what the reader does (UID-only vs crypto, writes-back). Saves a report. Authorized use only.",
    fap_version="1.0",
    fap_icon="icon.png",
)
